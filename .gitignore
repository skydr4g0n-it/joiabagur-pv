# .NET / ASP.NET Core
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
*.userprefs
[Dd]ebug/
[Rr]elease/
x64/
x86/
[Aa]rm/
[Aa]rm64/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/
*.csproj.user
*.csproj.vspscc
*.csproj.user
*.suo
*.user
*.userosscache
*.sln.docstates
.vs/
.vscode/
*.swp
*.cache
*.dll
*.exe
*.pdb
*.cache

# Build results
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

# Visual Studio
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
*.userprefs

# Rider
.idea/
*.sln.iml

# User-specific files
*.rsuser
*.suo
*.user
*.userosscache
*.sln.docstates

# Build results
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

# .NET Core
project.lock.json
project.fragment.lock.json
artifacts/
**/Properties/launchSettings.json

# ASP.NET Scaffolding
ScaffoldingReadMe.txt

# Files built by Visual Studio
*_i.c
*_p.c
*_h.h
*.ilk
*.meta
*.obj
*.iobj
*.pch
*.pdb
*.ipdb
*.pgc
*.pgd
*.rsp
*.sbr
*.tlb
*.tli
*.tlh
*.tmp
*.tmp_proj
*_wpftmp.csproj
*.log
*.vspscc
*.vssscc
.builds
*.pidb
*.svclog
*.scc

# Frontend - Node.js / React / Vue / Angular
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Testing
coverage/
*.lcov
.nyc_output

# Production
build/
dist/
out/
.next/
.nuxt/
.vuepress/dist/
.serverless/
.fusebox/
.dynamodb/
.tern-port

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local
# Secretos locales junto al compose (plantilla: backend/.env.example).
backend/.env

# Vite
dist/
dist-ssr/
*.local

# Docker
#
# `.dockerignore` used to be listed here and no longer is (C17). A build
# context exclusion file is CONFIGURATION, on the same footing as the Dockerfile
# beside it: ignoring it means every build runs with whatever exclusions the
# machine happens to have, which is how the bundled image ended up shipping a
# 1.18 GB context nobody noticed. Two are tracked now — one at the repository
# root and one in `ai-service/`, because a `.dockerignore` only governs the
# context it sits at the root of.
docker-compose.override.yml
*.dockerfile.local

# PostgreSQL
*.sql.backup
*.dump
pgdata/
postgres-data/

# Uploads / Storage (local development)
uploads/
temp/
tmp/
*.tmp

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# IDE - Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# IDE - JetBrains Rider
.idea/
*.sln.iml

# Secrets and sensitive files
*.key
*.pem
*.p12
*.pfx
*.jks
secrets.json
*.secrets.json
Documentos/Guias/aws-production-credentials.md

# Local developer overrides — personal, never committed.
# The dividing line in this repository: `appsettings*.json` are CONFIGURATION and are versioned,
# including the development placeholders they already carry (local database password, local JWT
# key). Real secrets never live in the repository: in production they arrive as environment
# variables from SSM Parameter Store, and for local work the right home is .NET user-secrets
# (`dotnet user-secrets set`), which stores them in the user profile, outside the working tree.
# Anything you need to override on your own machine goes in one of these files.
appsettings.Local.json
appsettings.*.Local.json

# Azure / AWS
.azure/
.aws/

# ML Models (if large)
*.h5
*.pb
*.onnx
models/
*.model

# Temporary files
*.swp
*.swo
*~
.cache/

# Contexto generado por la skill generate-pr (manifest, chunks del diff, body)
.pr/

# Contexto generado por la skill update-docs (manifest, resumen, plan)
.docs-update/

# Catálogo real de tienda: SKU, nombres y PRECIOS reales de un negocio real.
# Este repositorio es PÚBLICO. El export crudo no se versiona nunca; lo que
# C06 publique será el derivado anonimizado (data_origin: real).
data/catalog/real/*
!data/catalog/real/.gitkeep
# C06a: derivado anonimizado (JSONL + sidecar). El xlsx y el backup SQL
# siguen ocultos por el patrón de arriba.
!data/catalog/real/generated/
!data/catalog/real/generated/**

# C06b: corpus sintético versionado (JSONL + sidecar). Nada más bajo synthetic/.
data/catalog/synthetic/*
!data/catalog/synthetic/.gitkeep
!data/catalog/synthetic/generated/
!data/catalog/synthetic/generated/**

# C10: YAML de perfiles commiteado; JSONL de ventas y dumps locales no.
data/world/generated/*
!data/world/generated/.gitkeep
data/world/backups/*
!data/world/backups/.gitkeep

# Test run artifacts (dotnet test --logger trx)
TestResults/
