# Tasks — add-hybrid-search-rrf (C21)

Order is dependency order. Groups 1-4 are the cut line from the ticket: lexical branch, fusion, real provenance and honest modes are archivable on their own. Group 5 is the demoting filters, group 6 the singleton, group 7 the panel, group 8 the evidence.

## 1. Safe composition of the lexical query

- [x] 1.1 Create `ai-service/src/jbg_ai/retrieval/lexical.py` and move the safe composition shape out of `measure.py`: one `plainto_tsquery` per emitted surface form, forms OR-ed inside a group, **groups OR-ed with each other** (not `&&`), every term a bound parameter and no query syntax concatenated. Validate: a unit test asserts the generated fragment has one placeholder per surface form and that no operator text appears in the SQL string.
- [x] 1.2 Compose the typed list with `websearch_to_tsquery` over the raw query text, so the operator gets `"quotes"` and `-negation` for free. Validate: a test asserts a quoted phrase and a negated term reach the generated query, and that a malformed input does not raise.
- [x] 1.3 Classify each expansion group as counting or not counting towards coordination, from `ExpandedQuery.matched[].field`: `piece_type` and `materials` count, an unresolved group counts, and `occasion_tags`, `style_tags`, `color_tags` and `size_label` do not. Keep the sparse-field set as a module constant with its measured coverage in the comment (13 %, 11 %, 19 %, 45 %), never as settings. Validate: `test_sparse_vocabulary_fields_do_not_count_towards_coordination` and `test_unresolved_term_counts_towards_coordination`.
- [x] 1.4 Build the coordination expression `Σ (tsv @@ group_i)::int` over the counting groups only, and the ordering `coordination DESC, ts_rank DESC`. Validate: `test_lexical_branch_ors_groups_and_ranks_by_coordination` and `test_group_matching_nothing_does_not_change_order`.
- [x] 1.5 Pin the two constructors that were measured: `plainto_tsquery` for every emitted surface form and never `phraseto_tsquery`. Validate: `test_surface_forms_use_plainto_not_phraseto`, citing the measurement (`aro de dedo` 6 documents against 0).

## 2. The lexical branch on the search port

- [x] 2.1 Extend `ProductSearchPort` in `retrieval/ports.py` with a lexical method and a hit type carrying `product_id`, `sku`, `materials`, `family_id`, `variant_label`, `ts_rank` and the coordination count. Keep the port free of SQL. Validate: the protocol type-checks and the existing fake in `tests/support/fake_product_search.py` implements it.
- [x] 2.2 Implement the lexical SQL in `retrieval/search.py` over `ai.product_document`, filtered by `is_active`, applying the same body filters the vector branch applies, ordered by coordination then `ts_rank`, limited to the branch depth. Do not read schema `public`. Validate: a compile test asserts the statement references `tsv` and the GIN-indexed predicate, and applies each body filter.
- [x] 2.3 Extend the in-memory fake with lexical rows so the whole suite stays offline. Validate: `tests/retrieval/test_search_port.py` covers coordination ordering and body filters without a database.

## 3. Fusion

- [x] 3.1 Create `ai-service/src/jbg_ai/retrieval/fusion.py` with a pure weighted RRF over N ranked lists, returning the fused order plus provenance per candidate (which lists, at which rank). No database, no provider, no domain knowledge. Validate: `test_rrf_fuses_ranked_lists_preserving_top_hit` and a purity test asserting no socket and no session.
- [x] 3.2 Truncate every list at the **same** branch depth before fusing, and keep that depth separate from the over-retrieval window. Validate: `test_branch_depth_is_symmetric_across_lists` and `test_branch_depth_is_independent_of_overfetch`.
- [x] 3.3 Add the settings `JPV_RRF_K`, `JPV_RRF_WEIGHT_TYPED`, `JPV_RRF_WEIGHT_EXPANDED`, `JPV_RRF_WEIGHT_VECTOR` and `JPV_BRANCH_DEPTH` with the measured defaults (60, 0.5, 0.5, 0.33, 60), optional at boot, and pin all five in `canonical_openapi_settings`. Validate: `test_fusion_weights_and_k_load_from_settings_not_hardcoded` plus the runtime scenarios on health booting without them.
- [x] 3.4 Pass the effective values as parameters of `retrieve_products`, the same seam `settings`, `embed`, `search` and `expand_synonyms` already use, so C24 can sweep in one process. Validate: two calls with different weights in one process, neither mutating the settings object.
- [x] 3.5 Pin the measured default that is easiest to undo by accident: the vector weight must stay below either lexical weight. Validate: `test_vector_branch_weight_defaults_below_lexical`, with the 96/120 against 105/120 figures in the test docstring.
- [x] 3.6 Verify the exact degradation when expansion is disabled: the two lexical lists become identical and the fused order equals one lexical list at combined weight. Validate: `test_disabled_expansion_degrades_to_single_lexical_vote`.

## 4. Orchestration, provenance and honest modes

- [x] 4.1 Wire `retrieve_products` to consume `ExpandedQuery`: run the lexical branch concurrently with the embedding call — not with the vector search — so one pool connection is held at a time against `pool_size=5, max_overflow=0`. Validate: `test_lexical_query_runs_concurrently_with_embedding`.
- [x] 4.2 Make `mode` honest: `lexical` makes no provider call, `vector` does not query `tsv`, `hybrid` fuses all three. Remove `VECTOR_UNTIL_C21_NOTE` and its emission. Validate: `test_lexical_mode_makes_no_provider_call` and a test asserting `vector_only_until_c21` no longer appears in any response.
- [x] 4.3 Populate `match_reasons` from the fusion provenance (`vector`, `lexical` or both) instead of the literal `["vector"]`, and fill `debug.vector_score` and `debug.lexical_score` only for the branches that saw the candidate. Validate: `test_match_reasons_report_real_provenance` and a test asserting an absent diagnostic is `None`, never fabricated.
- [x] 4.4 Change `score` to the fused RRF score normalised so the first result is 1.0, staying inside `[0, 1]` and monotone with the order. Validate: a test asserting the first result scores 1.0 and the sequence is non-increasing.
- [x] 4.5 Degrade honestly on provider failure in `hybrid`: 200 with lexical-only candidates when the lexical branch produced any, 503 when it did not. Validate: `test_embedding_failure_in_hybrid_degrades_to_lexical` and `test_embedding_failure_with_no_lexical_hits_is_503`.
- [x] 4.6 Redefine `low_confidence` as the absence of any candidate produced by more than one branch, as a signal only: it must not change how many results are returned or their order. Apply that meaning **only when more than one branch actually ran** — in a single-branch mode, and in a `hybrid` request degraded by a provider failure, no candidate can appear twice, so the rule would mark every response low confidence and the field would carry no information; there it keeps the C14 meaning of "nothing was returned". Report the branches that ran in `stage=fuse`. Validate: `test_low_confidence_signals_absence_of_cross_branch_consensus`, `test_single_branch_modes_do_not_report_permanent_low_confidence`, `test_a_degraded_hybrid_response_is_not_marked_low_confidence` and `test_the_fuse_log_names_the_branches_that_actually_ran`.
- [x] 4.7 Emit `stage=lexical`, `stage=filters` and `stage=fuse` with `trace_id`, beside the existing `stage=expand`, `stage=embed` and `stage=search`. Report list sizes and cross-branch overlap in the fusion entry; keep the operator query at Debug and never log vectors. Validate: a caplog test asserting the three entries and their fields.
- [x] 4.8 Update the `stage=expand` entry so it no longer reports the expansion as unconsumed. Validate: the entry's `consumed` field is true when the lexical branch ran.

## 5. Structural filters that demote

- [x] 5.1 Create `ai-service/src/jbg_ai/retrieval/filters.py` extracting a price ceiling, a size and materials from the query text **by rule**, reusing `ExpandedQuery.matched` as the lookup instead of building a second one. Validate: `test_extracts_price_ceiling_from_natural_phrase` and `test_never_invents_filter_absent_from_query`.
- [x] 5.2 Apply the extracted constraints as a **stable block sort** after fusion — `(over_ceiling, size_mismatch, material_mismatch, −rrf_score)` — preserving the fused order inside each block and never removing a candidate from the over-retrieval window. Validate: `test_structural_filter_demotes_but_never_removes`.
- [x] 5.3 Keep body filters excluding, unchanged: `&&` for materials, equality for category and family, and never `@>`. Validate: `test_body_filters_remain_hard`, `test_material_filter_uses_overlap_by_default` and `test_multi_material_query_uses_contains_all` — the last one inverted with its measurement (60 against 913) so it pins the exclusion of `@>` rather than its use.
- [x] 5.4 Do not filter by extracted `piece_type` at all, and do not exclude by price or stock anywhere. Validate: a test asserting the compiled SQL of both branches contains no `price` predicate, and one asserting an extracted piece type produces no `WHERE`.
- [x] 5.5 Report the extracted constraints in `debug.notes` and in `stage=filters`, so C24 can audit whether the rules fired correctly before the contract ever moves to hand them to .NET. Validate: a test asserting the note for a query with a price ceiling.

## 6. Embedding client singleton with a bounded cache

- [x] 6.1 Define a bounded LRU cache in `retrieval/` that satisfies the `InMemoryEmbeddingCache` interface, **without editing** `indexing/embeddings.py`, frozen since C11. Validate: `test_embedding_cache_is_bounded` — inserting more entries than the ceiling evicts rather than grows — and `test_embeddings_module_is_unchanged`.
- [x] 6.2 Build the retrieval embedding client once in `api/main.py`, injecting the bounded cache, and resolve it from `app.state` in `api/routers/retrieval.py` instead of constructing one per request. Keep the injection seam tests use. Validate: `test_retrieval_embed_client_is_a_process_singleton`.
- [x] 6.3 Leave `AiGateway:RetrievalTimeoutMs` at 2500 ms and update the `openspec/DEFERRED_TASKS.md` entry: singleton paid, budget revert still open, with the new figures as the baseline to beat. Validate: `backend/` has no diff and the deferred entry names the follow-up change.

## 7. Panel: origin badge per result

- [x] 7.1 Derive the origin badge in `frontend/src/components/sales/assisted-search-result-row.tsx` from that result's `matchReasons` instead of the response-wide `aiAvailable`, keeping `ORIGIN_LABELS` as the lookup C16 left ready and falling back to the neutral label for an unknown value. Validate: `should show the lexical origin badge when a result has no vector provenance` and `should show the assisted badge when the result came from both branches`.
- [x] 7.2 Confirm no .NET work is needed: `MatchReasons` already travels from `AssistedSearchService` to the DTO and to `ai-search.types.ts`. Validate: `backend/` has no diff and `npm run build` is green.
- [x] 7.3 State the search's own mode at response level, derived from the results' provenance rather than from a new field: a response the service served **without** its semantic branch — the embedding provider failed and it degraded to lexical — must say so, because it arrives as HTTP 200 with results on screen and `aiAvailable` true, and is therefore invisible. It must not be presented as the assisted search being unavailable, which is a different state with its own message. Validate: `should say the semantic branch did not run when only lexical results came back`, `should not warn about the semantic branch when it did run` and the `searchOrigin` unit tests.

## 8. Evidence, documentation and gates

- [x] 8.1 Extend `retrieval/measure.py` with the configuration comparison — vector only, lexical only and the fused default — over the curated and recorded queries, writing a versioned report to `ai-service/evals/results/`. It must skip cleanly when no database is reachable and never be part of the unit suite. Validate: the report is written and `test_measure.py` still passes without a database.
- [x] 8.2 Update `ai-service/README.md`: rows for the five fusion settings in the environment table, a C21 paragraph, and the two declared behaviour changes — `score` changes scale so figures across C21 are not comparable, and the distance threshold does not discriminate between plausible queries.
- [x] 8.3 Link HU-AIENG-021 in `Documentos/epicas.md` (EP14) and mark C21 as done once implemented.
- [x] 8.4 Run the full gate: `uv run pytest` green with no provider, LLM or RDS socket; `test_openapi_snapshot_is_stable` green with `ai-service/openapi.json` unchanged; `npm run build` green; frontend suite compared by failing **test names** against the documented red baseline, never by count; `openspec validate --all --strict` at `0 failed`.
- [ ] 8.5 Verification after merge, not a merge gate: run the measurement CLI against the local index and confirm it reproduces `sortija de plata` 4/10 → 10/10 and `criollas de oro` 1/10 → 6/10, and measure the full pipeline latency cold and warm to leave the baseline that will decide the 800 ms revert in the next change.
